apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    updateIntervalSeconds: 20
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      extensions:
        - json